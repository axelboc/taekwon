/* eslint-disable */
const state = {
  rings: [
    {
      isOpen: boolean,
      isFull: boolean
    }
  ],
  socket: { // reducer
    isConnected: boolean,
    isReconnecting: boolean
  },
  status: { // reducer
    //ring: number || null
  }
};
