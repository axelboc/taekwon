/* eslint-disable */
const state = {
  rings: [
    {
      isOpen: boolean,
    }
  ],
  socket: { // reducer
    isConnected: boolean,
    isReconnecting: boolean
  },
  status: { // reducer
    //ringOpened: number || null
  }
}
